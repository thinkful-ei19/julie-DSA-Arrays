'use strict';

const exampleArr = [1, 3, 9, 4];

function products(arr, num) {
    for(var i = 0; i < arr.length; i++) {
        let firstIndex = arr.substring(i, i+1);
        let remainingIndex = arr.substring(i+1);
        
    }
}

console.log(products(exampleArr))